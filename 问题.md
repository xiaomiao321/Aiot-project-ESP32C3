问题：WIFI老是连不上——关闭省电模式，用手摸着芯片可以很大程度上增大连上的几率，同时配置WIFI

```cpp
WiFi.mode(WIFI_STA);
WiFi.setAutoReconnect(true);
WiFi.persistent(true);
WiFi.setSleep(false);
WiFi.setTxPower(WIFI_POWER_19_5dBm);
WiFi.begin(saved_ssid.c_str(), saved_pass.c_str());
```



华为云连不上：
报错：![DNS_Failed](D:\files\25_26_Autumn\AIoT_final\img\DNS_Failed.png)

解决：关了手机省电模式，连上热点就能连上了，很诡异